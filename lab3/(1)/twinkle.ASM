;用定时器1的方式1定时，P1.0接一发光二极管，使该发光二极管每秒钟闪烁5次（采用中断方式）。
	ORG 0000H
	LJMP MAIN 
	ORG 001BH
	LJMP INT
	ORG 0100H
MAIN:CLR P1.0
	MOV B,#02H
	MOV TMOD,#10H
	MOV TH1,#3CH
	MOV TL1,#0B0H
	SETB TR1	;??T1
	SETB ET1	;?T1??
	SETB EA	
	SJMP $
		
INT:MOV TH1,#3CH
	MOV TL1,#0B0H
	DJNZ B,LOOP
	CPL P1.0
	MOV B,#02H
LOOP:RETI

END
